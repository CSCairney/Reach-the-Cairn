import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
    title: "Details | Dashboard",
    description: 'Personal details page for editing and review',
  };

const Details = () => {
    return (
        <div>
            <h1>Details Page</h1>
        </div>
    );
}

export default Details;