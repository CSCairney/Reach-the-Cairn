import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
    title: "Reach the Health",
    description: 'A door into the physical & mental health of reach the cairn community',
  };
  
const Health = () => {
    return (
        <div>
            <h1>Reach the Health</h1>
        </div>
    );
}

export default Health;